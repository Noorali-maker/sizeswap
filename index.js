import React from 'react';
import ReactDOM from 'react-dom/client';
import SizeSwapApp from './SizeSwapApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SizeSwapApp />
  </React.StrictMode>
);
